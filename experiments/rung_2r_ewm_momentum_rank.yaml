# Rung 2r: EWM momentum (h=60d, skip-month) + rank-based dual-softmax

experiment_id: rung_2r_ewm_momentum_rank

factor:
  type: ewm_mom

portfolio:
  type: dual_softmax_rank
  gross_leverage: 1.0

universe:
  filters: [in_top_2000, price_above_5, adtv_above_5m]

rolling:
  start_year: 2010
  end_year:   2023
  train_years: 0
  val_years:   0

output_dir: outputs/rung_2r_ewm_momentum_rank/
